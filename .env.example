DATABASE_URL="postgresql://postgres:postgres@localhost:5432/homelink"
# When DATABASE_URL points to PostgreSQL, the full marketplace store snapshot is persisted there.
# Without PostgreSQL, operational data falls back to .data/app-store.json on disk.
# Local-only SQLite settings mirror. Do not set this to file: storage in strict production.
# Strict production should rely on the main Postgres-backed app snapshot or a durable settings database.
SETTINGS_DATABASE_URL="file:./.data/settings.db"
NEXT_PUBLIC_APP_URL="https://homelinkzim.co.zw"
# Set to true in the final production environment. This makes unsafe launch
# defaults fail closed instead of silently using local/sandbox fallbacks.
HOMELINK_STRICT_PRODUCTION="false"
# Used to sign local-session cookies. Set a long random value in production.
HOMELINK_SESSION_SECRET=""

# Configure one provider when auth is implemented.
CLERK_SECRET_KEY=""
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
FIREBASE_PROJECT_ID=""

CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

SMTP_HOST="smtp.resend.com"
SMTP_PORT="587"
SMTP_USER="resend"
SMTP_PASS=""
SMTP_FROM="support@homelinkzim.co.zw"
# Optional alias: if set, RESEND_API_KEY is used as SMTP_PASS when SMTP_PASS is blank.
RESEND_API_KEY=""

STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
PAYNOW_INTEGRATION_ID=""
PAYNOW_INTEGRATION_KEY=""

# Local seed credentials. Set private values locally and in production; never commit real passwords.
SEED_STANDARD_PASSWORD=""
SEED_TINASHE_PASSWORD=""
SEED_LANDLORD_PASSWORD=""
SEED_ADMIN_PASSWORD=""
SEED_CONSULTANT_PASSWORD=""
